<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Settings</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center">Customize Weather Display</h1>
        <form action="/save-settings" method="POST">
            <div class="form-check mb-3">
                <input class="form-check-input" type="checkbox" id="showTemperature" name="showTemperature" 
                       <% if (preferences.showTemperature) { %> checked <% } %>>
                <label class="form-check-label" for="showTemperature">Show Temperature</label>
            </div>
            <div class="form-check mb-3">
                <input class="form-check-input" type="checkbox" id="showPressure" name="showPressure" 
                       <% if (preferences.showPressure) { %> checked <% } %>>
                <label class="form-check-label" for="showPressure">Show Pressure</label>
            </div>
            <div class="form-check mb-3">
                <input class="form-check-input" type="checkbox" id="showWindSpeed" name="showWindSpeed" 
                       <% if (preferences.showWindSpeed) { %> checked <% } %>>
                <label class="form-check-label" for="showWindSpeed">Show Wind Speed</label>
            </div>
            <div class="form-check mb-3">
                <input class="form-check-input" type="checkbox" id="showHumidity" name="showHumidity" 
                       <% if (preferences.showHumidity) { %> checked <% } %>>
                <label class="form-check-label" for="showHumidity">Show Humidity</label>
            </div>
            <div class="form-check mb-3">
                <input class="form-check-input" type="checkbox" id="showUvIndex" name="showUvIndex" 
                       <% if (preferences.showUvIndex) { %> checked <% } %>>
                <label class="form-check-label" for="showUvIndex">Show UV Index</label>
            </div>
            <div class="form-check mb-3">
                <input class="form-check-input" type="checkbox" id="showAirQuality" name="showAirQuality" 
                       <% if (preferences.showAirQuality) { %> checked <% } %>>
                <label class="form-check-label" for="showAirQuality">Show Air Quality</label>
            </div>
            <div class="form-check mb-3">
                <input class="form-check-input" type="checkbox" id="showAlerts" name="showAlerts" 
                       <% if (preferences.showAlerts) { %> checked <% } %>>
                <label class="form-check-label" for="showAlerts">Show Weather Alerts</label>
            </div>
            <button type="submit" class="btn btn-primary">Save Preferences</button>
        </form>
    </div>
</body>
</html>
