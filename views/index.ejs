<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather App</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <div class="container">
        <div class="text-center mt-5">
            <h1 class="display-4">Weather Forecast</h1>
            <p class="lead">Get accurate weather data, including pressure details, from multiple sources.</p>
        </div>

        <div class="card mx-auto mt-4" style="max-width: 600px;">
            <div class="card-body">
                <h5 class="card-title text-center mb-4">Check the Weather</h5>
                <form action="/forecast" method="GET">
                    <div class="input-group mb-3">
                        <input type="text" class="form-control" id="city" name="city" placeholder="Enter City or Location..." required>
                        <button type="submit" class="btn btn-primary">Get Forecast</button>
                    </div>
                    <div class="mb-3">
                        <label for="days" class="form-label">Select Forecast Days:</label>
                        <select class="form-control" id="days" name="days">
                            <option value="3">3 Days</option>
                        </select>
                    </div>
                </form>

                <button id="use-location" class="btn btn-outline-secondary w-100">Use My Location</button>
            </div>
        </div>

        <% if (recentSearches.length > 0) { %>
        <div class="mt-5">
            <h5>Recent Searches</h5>
            <ul class="list-group">
                <% recentSearches.forEach(city => { %>
                    <li class="list-group-item">
                        <a href="/forecast?city=<%= city %>&days=3" class="text-decoration-none text-dark"><%= city %></a>
                    </li>
                <% }) %>
            </ul>
        </div>
        <% } %>
    </div>

    <script>
        document.getElementById('use-location').addEventListener('click', () => {
            if (navigator.geolocation) {
                navigator.geolocation.getCurrentPosition(async (position) => {
                    const lat = position.coords.latitude;
                    const lon = position.coords.longitude;

                    // Redirect to the forecast page with the user's location
                    window.location.href = `/forecast?city=${lat},${lon}&days=3`;
                });
            } else {
                alert('Geolocation is not supported by your browser.');
            }
        });
    </script>
</body>
</html>
